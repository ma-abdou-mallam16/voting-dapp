import { Alert, AlertIcon, AlertTitle, AlertDescription } from '@chakra-ui/react'
const Finished = () => {

  return (
    <Alert status='warning'>
        <AlertIcon />
    No action can be done right now.
    </Alert>
  )
}

export default Finished;